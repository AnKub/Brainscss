{{#with data}}

<div class="card">
    <div class="card_image" style="background-image: url({{photo}})"></div>
    <h2 class="card_title">{{name}}</h2>
    <p class="card_text">{{description}}</p>
    <button href="#" class="card_button">Read All</button>
</div>

{{/with}}